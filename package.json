{
  "name": "amplifier-onboarding",
  "version": "0.1.0",
  "description": "Amplifier web experience - showcase and sandbox for AI-native development",
  "scripts": {
    "dev": "npx http-server . -p 3000 -o -c-1",
    "start": "npm run dev",
    "preview": "npx http-server . -p 3000",
    "build": "echo 'No build step needed - pure static site'",
    "build:azure": "echo 'No build step needed - pure static site'",
    "validate": "node scripts/validate-recipes.js",
    "validate:schema": "node scripts/validate-recipes.js",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:ui": "npx playwright test --ui",
    "test:debug": "npx playwright test --debug"
  },
  "keywords": [
    "amplifier",
    "ai",
    "workflows",
    "recipes",
    "ai-development"
  ],
  "author": "Microsoft",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
